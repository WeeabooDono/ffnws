{% extends 'base.html.twig' %}

{% block body %}

    <h1>UtilisateurRole</h1>

    <table>
        <tbody>
            <tr>
                <th>Nomequipe</th>
                <td>{{ utilisateurrole.idRole.nom }}</td>
            </tr>
            <tr>
                <th>Nomepersonne</th>
                <td>{{ utilisateurrole.idPersonne.nom }}</td>
            </tr>
            <tr>
                <th>Nomcompetition</th>
                <td>{{ utilisateurrole.idCompetition.nom }}</td>
            </tr>

        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('utilisateurrole_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('utilisateurrole_edit', { 'idRole': utilisateurrole.idRole.idRole, 'idPersonne': utilisateurrole.idPersonne.idPersonne, 'idCompetition': utilisateurrole.idCompetition.idCompetition }) }}">Edit</a>
        </li>
        <li>Delete
            {#
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
            #}
        </li>
    </ul>

{% endblock %}
